backend:
  name: github
  repo: nikhilweee/nweeeblog
  branch: main # Branch to update (optional; defaults to master)
  base_url: http://decap.nikhilweee.me # Base URL for OAuth proxy

local_backend: true
publish_mode: editorial_workflow
media_folder: "static/images/uploads" # Media files will be stored in the repo under static/images/uploads
public_folder: "/images/uploads" # The src attribute for uploaded media will begin with /images/uploads
site_url: https://nikhilweee.me

slug:
  encoding: "ascii"
  clean_accents: true
  sanitize_replacement: "_"

collections:
  - name: "blog" # Used in routes, e.g., /admin/collections/blog
    label: "Blog" # Used in the UI
    folder: "content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    preview_path: "blog/{{year}}/{{slug}}"
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Subtitle", name: "subtitle", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Categories", name: "categories", widget: "list" }
      - { label: "Draft", name: "draft", widget: "boolean", default: true }
      - { label: "Katex", name: "katex", widget: "boolean", default: false }
      - { label: "Citations", name: "citations", widget: "boolean", default: false }
      - { label: "Body", name: "body", widget: "markdown" }
    sortable_fields: ["date", "title"]
    view_groups:
      - label: Year
        field: date
        pattern: \d{4}
